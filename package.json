{
  "name": "replyify",
  "version": "0.0.5",
  "description": "A free Gmail auto-reply app with follow-up emails",
  "main": "./dist/index.js",
  "scripts": {
    "test": "npm run prepare && ./node_modules/mocha/bin/mocha test/ --recursive --timeout=10000",
    "prepare": "node -e 'require(`prepare-package`)'"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/replyify/replyify"
  },
  "keywords": [
    "replyify",
    "gmail auto-reply",
    "gmail follow-up emails",
    "nodejs",
    "front end"
  ],
  "author": "ITW Creative Works",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/replyify/replyify/issues"
  },
  "homepage": "https://replyify.app",
  "devDependencies": {
    "mocha": "^8.4.0",
    "prepare-package": "^0.0.12"
  }
}